{
  "name": "pet-memorial",
  "version": "1.0.0",
  "description": "Pet Memorial Website",
  "main": "index.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "build": "mkdir -p dist && cp -r public dist/ && cp server.js package.json package-lock.json dist/ && cp -r data dist/ 2>/dev/null || true"
  },
  "dependencies": {
    "express": "^4.18.2",
    "multer": "^1.4.5-lts.1",
    "uuid": "^9.0.0"
  },
  "devDependencies": {
    "nodemon": "^3.0.1"
  }
}